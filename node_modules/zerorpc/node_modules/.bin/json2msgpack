#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../msgpack/bin/json2msgpack" "$@"
  ret=$?
else 
  node  "$basedir/../msgpack/bin/json2msgpack" "$@"
  ret=$?
fi
exit $ret
