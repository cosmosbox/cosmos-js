#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../axon-rpc/bin/arpc" "$@"
  ret=$?
else 
  node  "$basedir/../axon-rpc/bin/arpc" "$@"
  ret=$?
fi
exit $ret
